[build]
  command = "npm run build"
  publish = "dist"

# SPA routing için - tüm route'ları index.html'e yönlendir
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
